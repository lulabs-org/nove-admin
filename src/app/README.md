# App Directory

应用层目录，负责组装和配置。

## 子目录说明

- **layout/** - 布局组件（主布局、侧边栏、头部等）
- **routes/** - 路由配置和路由组件
- **guards/** - 路由守卫（权限验证、登录状态等）
- **providers/** - 应用级 Provider（React Query、路由、主题等）
- **config/** - 应用配置文件

## 原则

- 只负责组装，不包含业务逻辑
- 不包含可复用的通用组件
- 保持简洁，便于维护
